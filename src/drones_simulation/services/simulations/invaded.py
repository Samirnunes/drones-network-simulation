from ...models.base import BaseSimulation


class InvadedSimulation(BaseSimulation):

    def run(self) -> None:
        # logger.info("Starting simulation...")
        # steps = self._behavior.steps()
        # while True:
        #    next(steps)
        pass
